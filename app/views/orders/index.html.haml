.orders
  .orders__detail
    .orders__detail__title
      注文履歴
    .orders__detail__history
      - @orders.each do |order|
        = link_to order.created_at.strftime("%Y/%m/%d(%a) %H:%M"), user_order_orderitems_path(current_user, order) 
        %br